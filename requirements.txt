# Python dependencies for GenLayer Oracle project
#
# IMPORTANT: genlayer-py requires Python >=3.12
# If using Python <3.12, you may encounter import errors.
# Use 'pip install --upgrade python' or use Python 3.12+

# GenLayer Python SDK
# Requires Python >=3.12 (Buffer from collections.abc is Python 3.12+)
genlayer-py>=0.1.0

# Optional: For API key encryption (if using encrypted on-chain pattern)
cryptography>=41.0.0

# Optional: For additional utilities
requests>=2.31.0

